# Local: variables separadas
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clinica
DB_USER=admin
DB_PASSWORD=admin123
JWT_SECRET=medlife_secret_2024

# Vercel / Neon / Supabase: usa la URL completa (recomendado)
# DATABASE_URL=postgresql://usuario:password@host:5432/clinica?sslmode=require
